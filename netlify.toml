[build]
  command = "corepack enable && pnpm install --frozen-lockfile=false && pnpm --filter @wherehouse/web build"
  publish = "apps/web/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
