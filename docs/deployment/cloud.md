# Cloud deployment

Current notes live in [`deploy/cloud/README.md`](../../deploy/cloud/README.md). Cloud uses the same
versioned API and capability behavior as self-hosting. Managed PostgreSQL and intentional
S3-compatible media storage may replace local implementations, but cloud-only dependencies must not
enter core workflows. Cloud deployment does not substitute for the MVP's supported local/Pi path.
