# METABEGIN
# X-Env-Layer-Name: wherehouse-appliance
# X-Env-Layer-Desc: WhereHouse local-first appliance runtime
# X-Env-Layer-Category: application
# X-Env-Layer-Requires: systemd-min
# METAEND
---
mmdebstrap:
  packages:
    - avahi-daemon
    - ca-certificates
    - curl
    - docker.io
    - docker-cli
    - docker-compose
    - openssh-server
    - sudo
